/**
 * brand.ts — the app's name.
 *
 * "Lodestar" is the platform/suite brand; THIS app is the construction
 * command center, so it's "Construction Lodestar". Future ventures each get
 * their own Lodestar (e.g. a Stonebridge kitchen-and-bath one), launched from
 * the same `lodestar` chooser. One place to change the name.
 */
export const APP_NAME = 'Construction Lodestar'
export const APP_ICON = '⭐'
